appId: 'com.ApocalypticSurvival.launcher'
productName: 'Dystopian Launcher'
artifactName: '${productName}-setup-${version}.${ext}'

copyright: 'Copyright 2025 Dystopian'

asar: true
compression: 'maximum'

files:
  - '!{dist,.gitignore,.vscode,docs,dev-app-update.yml,.nvmrc,.eslintrc.json}'

extraResources:
  - 'libraries'

# Windows Configuration
win: 
  target: 
    - target: 'nsis'
      arch: 'x64'

# Windows Installer Configuration
nsis:
  oneClick: false
  perMachine: false
  allowElevation: true
  allowToChangeInstallationDirectory: true

# macOS Configuration
mac:
  target:
    - target: 'dmg'
      arch:
        - 'x64'
        - 'arm64'
  artifactName: '${productName}-setup-${version}-${arch}.${ext}'
  category: 'public.app-category.games'

# Linux Configuration
linux: 
  target: 'AppImage'
  maintainer: 'dystopian'
  vendor: 'dystopian'
  synopsis: 'Modded Minecraft Launcher'
  description: 'Launcher de Dystopian'
  category: 'Game'


directories:
  buildResources: 'build'
  output: 'dist'